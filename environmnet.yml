name: qml-cloud
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - numpy
  - scipy
  - pandas
  - scikit-learn
  - matplotlib
  - jupyterlab
  - pip
  - pip:
      - pennylane
      - pennylane-lightning   # optional fast plugin if you want
      - streamlit
